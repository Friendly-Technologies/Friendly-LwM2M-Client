# Mandatory objects must be present always
# therefore they do not need option for enable

# Update SOURCES variable from parent scope.
set(SOURCES ${SOURCES} ${CMAKE_CURRENT_SOURCE_DIR}/Security.cpp PARENT_SCOPE)